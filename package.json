{
  "name": "asistent-medical-ai-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "mobile:build": "next build",
    "mobile:init:android": "npm run mobile:build && npx cap add android && npx cap sync android",
    "mobile:init:ios": "npm run mobile:build && npx cap add ios && npx cap sync ios",
    "android": "npm run mobile:build && npx cap sync android && npx cap open android",
    "android:run": "npm run mobile:build && npx cap sync android && npx cap run android",
    "ios": "npm run mobile:build && npx cap sync ios && npx cap open ios",
    "ios:run": "npm run mobile:build && npx cap sync ios && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "lucide-react": "^0.468.0",
    "next": "^16.2.12",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.2"
  }
}
